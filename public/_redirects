# Redirect trailing slashes to non-trailing slash URLs (301 permanent redirects)
/about/ /about 301
/posts/*/ /posts/:splat 301

# Static pre-rendered pages for social media crawlers
# These serve the pre-rendered HTML with proper meta tags
/about /about/index.html 200
/posts/* /posts/:splat/index.html 200

# Fallback to SPA for client-side routing
/* /index.html 200